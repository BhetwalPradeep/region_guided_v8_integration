This directory intentionally does not store large model artifacts in Git.

Download the locked runtime files from:

s3://viewray-ai/Patient-vision/Pradeep/Region-aware-v8/model/

Expected files:
- checkpoint_best.weights.h5
- config.json
